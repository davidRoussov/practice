(ns clojure-practice.core
  ;(:use [clojure_practice.talking_clock])
  ;(:use [clojure_practice.collatz])
  (:use [clojure_practice.fibonacci])
  (:gen-class))


(defn -main
  [& args]
  ;(talking_clock [])
  ;(collatz [])
  (fibonacci [])
)